spring:
  profiles:
    active: ${PROFILE_ACTIVE:default}
  application:
    name: news-letter-ai
#  mail:
#    host: smtp.hostinger.com
#    port: 465
#    username: ${JSM_EMAIL}
#    password: ${JSM_PASSWORD}
#    properties:
#      mail:
#        smtp:
#          auth: true
#          starttls:
#            enable: false
#          ssl:
#            enable: true

logging:
  level:
    com.lucasmoraist: ${LOG_LEVEL}

secrets:
  brevo:
    api-url: https://api.brevo.com/v3
    api-key: ${SECRETS_BREVO_API_KEY}
  google:
    genai:
      api-key: ${SECRETS_GOOGLE_GENAI_API_KEY}

app:
  security:
    enabled: ${APP_SECURITY_ENABLED:true}
  security-ignore:
    actuatorHealth: /actuator/health
    actuatorLiveness: /actuator/health/liveness
    actuatorReadiness: /actuator/health/readiness
    customerSave: /api/v1/customer/save
  allowed-origins:
    - http://localhost:3000